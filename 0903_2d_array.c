/*
內容宣告:
以下程式碼由 李翼全 提供

二維陣列 
*/

/*
#include<stdio.h>
#include<stdlib.h>

int main()
{
	int a[2][3] = { {12,34,56},{78,910,11} };
	int vectors= sizeof(a)/sizeof(int); //元素個數
	int column = sizeof(a[0])/sizeof(int);//欄數
	int row = vectors / column; //元素除欄數=列數 
	
	printf("%d row, %d column, total: %d vectors", row, column,vectors);

	return 0;
}

*/ 
